{
    "extends": [
        "./tsconfig.json",
        // Different build options from other SDK packages because this generates the JavaScript, not just types
        // "../../tsconfig-build-options.json"
    ],
    "compilerOptions": {
        "noEmit": false,
        // Unlike other packages we emit JS (emitDeclarationOnly is false). Since
        // sources inherit allowImportingTsExtensions, emitting requires this (TS5096).
        "rewriteRelativeImportExtensions": true,
        "rootDir": "src",
        "outDir": "dist",
        "emitDeclarationOnly": false,
        "declaration": true,
        "declarationMap": true,
        "target": "es2022",
        "module": "es2022",
        "moduleResolution": "bundler",
        "lib": [
            "es2022",
            "DOM"
        ],
        "sourceMap": true,
        "isolatedModules": true,
    },
    "include": ["src"],
    "exclude": [
        "test",
    ]
}
