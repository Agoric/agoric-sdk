{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "strict": true,
    "noImplicitAny": false, // for compat with peer packages using ambient types
  },
  "include": [
    "src/**/*.js",
    "src/**/*.ts",
    "test/**/*.js",
  ]
}
