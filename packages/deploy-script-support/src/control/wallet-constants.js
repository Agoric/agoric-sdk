/**
 * Wallet store key where contract control is delivered.
 *
 * Kept local so deploy-script-support stays generic and does not depend on
 * higher-level app packages.
 */
export const CONTRACT_CONTROL_WALLET_KEY = 'ymaxControl';
