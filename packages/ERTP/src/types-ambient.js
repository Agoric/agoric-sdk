/// <reference types="ses"/>

/**
 * @typedef {import('@endo/marshal').InterfaceSpec} InterfaceSpec
 * @typedef {import('@endo/marshal').MarshalGetInterfaceOf} GetInterfaceOf
 */

/**
 * @typedef {import('./types').AdditionalDisplayInfo} AdditionalDisplayInfo
 * @typedef {import('./types').AmountValue} AmountValue
 * @typedef {import('./types').AssetKind} AssetKind
 * @typedef {import('./types').CopyBagValue} CopyBagValue
 * @typedef {import('./types').CopySetMathHelpers} CopySetMathHelpers
 * @typedef {import('./types').CopySetValue} CopySetValue
 * @typedef {import('./types').DepositFacet} DepositFacet
 * @typedef {import('./types').NatMathHelpers} NatMathHelpers
 * @typedef {import('./types').NatValue} NatValue
 * @typedef {import('./types').SetMathHelpers} SetMathHelpers
 * @typedef {import('./types').SetValue} SetValue
 * @typedef {import('./types').ShutdownWithFailure} ShutdownWithFailure
 */

/**
 * @template {AssetKind} [K=AssetKind]
 * @typedef {import('./types').Amount<K>} Amount
 */

/**
 * @template {AssetKind} [K=AssetKind]
 * @typedef {import('./types').DisplayInfo<K>} DisplayInfo
 */

/**
 * @template {AssetKind} [K=AssetKind]
 * @typedef {import('./types').Brand<K>} Brand
 */

/**
 * @template {AssetKind} [K=AssetKind]
 * @typedef {import('./types').Issuer<K>} Issuer
 */

/**
 * @template {AssetKind} [K=AssetKind]
 * @typedef {import('./types').Payment<K>} Payment
 */
/**
 * @template {AssetKind} [K=AssetKind]
 * @typedef {import('./types').IssuerKit<K>} IssuerKit
 */

/**
 * @template {AssetKind} [K=AssetKind]
 * @typedef {import('./types').Mint<K>} Mint
 */

/**
 * @template {AssetKind} [K=AssetKind]
 * @typedef {import('./types').Purse<K>} Purse
 */
