import React from 'react';
import Button from '@material-ui/core/Button';

const WithMaterialUI = () => {

  return (
    <div className="myDiv"><Button variant="contained" color="primary">
    Hello World
  </Button></div>
  );
};

export default WithMaterialUI;
