// Empty JS file to correspond with its .d.ts twin.
// The real types live in types-external.ts; this runtime-free pair lets the
// package's index.js re-export them with a plain `export *`.
export {};
