{
  "extends": "../../tsconfig.json",
  "compilerOptions": {},
  "include": [
    "*.js",
    "scripts",
    // XXX we need both globs or tsc will ignore the .d.ts twin
    "src/**/*.js",
    "src/**/*.ts",
    "test",
    "tools",
  ],
}
