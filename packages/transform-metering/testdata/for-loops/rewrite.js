const $m=getMeter();$m&&$m.e();try{for (const f of b) {$m && $m.c();
  doit(f);}

for (const p in bar) {$m && $m.c();
  doit(p);}

for (let i = 0; i < 3; i++) {$m && $m.c();
  doit(i);}
}finally{$m && $m.l();}
