#! /bin/bash
thisdir=$(dirname -- ${BASH_SOURCE[0]})
nodeFlags=()
while test $# -gt 0; do
  case "$1" in
  --inspect*) nodeFlags+=( "$1" ) ;;
  *) break ;;
  esac
  shift
done

exec node ${nodeFlags[@]} "$thisdir/../lib/ag-solo/entrypoint.js" ${1+"$@"}
