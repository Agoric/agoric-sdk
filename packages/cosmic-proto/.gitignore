# TODO gitignore build output and generate it as needed
# We don't ignore the build output (dist, mjs) because our CI integration
# tests don't have hooks to build the package before consuming it.

# cache
.tsimp

# Copy as needed from golang tree
proto
